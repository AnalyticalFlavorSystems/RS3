PKG_CXXFLAGS= -I../inst/include -Ilibs3/inc/mingw -Dsleep=Sleep -DSTRICT_R_HEADERS
PKG_LIBS=$(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS) -Llibs3/libs3-2.0/bin -ls3 

.PHONY: all
all: $(SHLIB)

$(SHLIB): libs3.dll

export CC

libs3.dll:
	echo $(SHLIB)
	(cd libs3; make -f Makefile.win)
    @echo $(PWD)
